<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- id는 구현했던 메소드와 동일하게 작성, resultType은 반환되는 값의 위치=값 -->
<mapper namespace="com.yedam.reply.mapper.ReplyMapper">
  <select id="replyList" resultType="reply" parameterType="int">
    select r.*,m.name name from reply r join member m on r.replyer = m.id where board_no=${bno}
  </select>
  <delete id="deleteReply" parameterType="int">
  	delete from reply where reply_no = #{replyNo}
  </delete>
  <insert id="insertReply" parameterType="reply">
  	<selectKey keyProperty="replyNo" resultType="int" order="BEFORE">
  	select reply_seq.nextval from dual
  	</selectKey>
  	insert into reply(reply_no,board_no,reply,replyer) VALUES (#{replyNo},#{boardNo},#{reply},#{replyer})
  </insert>
  <select id="selectReply" resultType="reply" parameterType="int">
  	select r.reply_no,r.board_no,r.reply,m.name name from reply r join member m on r.replyer = m.id where reply_no = ${replyNo}
  </select>
  <select id="replyListPaging" resultType="reply">
  	select b.* from
	(select ROWNUM rn, a.* from  
		(select 
			r.*,m.name name 
		from reply r join member m on r.replyer = m.id 
		where board_no = #{boardNo}
		order by 1
	) a ) b 
	<![CDATA[
	where b.rn > (#{page}-1)*5 and b.rn <= #{page}*5
	]]>
  </select>
  <select id="selectCount" resultType="int" parameterType="int">
  	select count(*) from reply where board_no = #{boardNo}
  </select>
  
  <!-- CDATA : 등호표시(><) 가능하게 만듦 -->
</mapper>